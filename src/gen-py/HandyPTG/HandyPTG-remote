#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.7.0-dev)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import HandyPTG
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host:port] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '   HPTGgetEvents(i32 sinceIndex)'
  print '  void HPTGdrawCard()'
  print '  void HPTGplayCard(string abbreviation, string index)'
  print '  void HPTGlandCard(string abbreviation, string index)'
  print '  void HPTGdiscardCard(string abbreviation, string index)'
  print '  string HPTGgetCard(string abbreviation, string index)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = HandyPTG.Client(protocol)
transport.open()

if cmd == 'HPTGgetEvents':
  if len(args) != 1:
    print 'HPTGgetEvents requires 1 args'
    sys.exit(1)
  pp.pprint(client.HPTGgetEvents(eval(args[0]),))

elif cmd == 'HPTGdrawCard':
  if len(args) != 0:
    print 'HPTGdrawCard requires 0 args'
    sys.exit(1)
  pp.pprint(client.HPTGdrawCard())

elif cmd == 'HPTGplayCard':
  if len(args) != 2:
    print 'HPTGplayCard requires 2 args'
    sys.exit(1)
  pp.pprint(client.HPTGplayCard(args[0],args[1],))

elif cmd == 'HPTGlandCard':
  if len(args) != 2:
    print 'HPTGlandCard requires 2 args'
    sys.exit(1)
  pp.pprint(client.HPTGlandCard(args[0],args[1],))

elif cmd == 'HPTGdiscardCard':
  if len(args) != 2:
    print 'HPTGdiscardCard requires 2 args'
    sys.exit(1)
  pp.pprint(client.HPTGdiscardCard(args[0],args[1],))

elif cmd == 'HPTGgetCard':
  if len(args) != 2:
    print 'HPTGgetCard requires 2 args'
    sys.exit(1)
  pp.pprint(client.HPTGgetCard(args[0],args[1],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
